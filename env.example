# Variables de entorno requeridas para Vercel deployment

# Google Calendar API (opcional pero recomendado)
GOOGLE_CLIENT_ID=tu_client_id_aqui.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=tu_client_secret_aqui
GOOGLE_REFRESH_TOKEN=tu_refresh_token_aqui
GOOGLE_REDIRECT_URI=https://tu-dominio.vercel.app

# Google Maps API (requerido para validación inteligente de rutas)
GOOGLE_MAPS_API_KEY=tu_google_maps_api_key_aqui

# Configuración de Admin
ADMIN_PASSWORD=tu_password_admin_seguro

# Configuración de Base de Datos (SQLite se incluye automáticamente)
NODE_ENV=production

# NOTA IMPORTANTE para Vercel:
# 1. Configura estas variables en Vercel Dashboard > Settings > Environment Variables
# 2. NO subas el archivo .env real al repositorio (está en .gitignore)
# 3. Vercel maneja automáticamente la base de datos SQLite 